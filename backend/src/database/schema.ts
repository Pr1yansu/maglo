// Database schema definitions
// Add your table schemas here as needed

// Example:
// export const exampleTable = pgTable('example', {
//   id: serial('id').primaryKey(),
//   name: varchar('name', { length: 255 }).notNull(),
// });

// For now, export empty schema
export {};
